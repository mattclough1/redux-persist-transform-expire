{
  "name": "redux-persist-transform-expire-immutable",
  "version": "0.0.1",
  "description": "Add expiration to your persisted (immutable) store",
  "main": "index.js",
  "scripts": {
    "test": "mocha ./test",
    "lint": "semistandard"
  },
  "keywords": [
    "redux",
    "redux-persist",
    "redux-persist-transform",
    "expire",
    "immutable"
  ],
  "author": "Gabriel Cebrian, Matt Clough",
  "license": "MIT",
  "devDependencies": {
    "expect.js": "^0.3.1",
    "mocha": "^2.5.3",
    "moment": "^2.13.0",
    "redux-persist": "^3.2.2",
    "semistandard": "^8.0.0"
  },
  "peerDependencies": {
    "redux-persist": ">=3.0"
  },
  "dependencies": {
    "immutable": "^3.8.1",
    "traverse": "^0.6.6"
  }
}
